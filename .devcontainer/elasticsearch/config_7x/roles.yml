hass_writer:
  cluster: ["manage_index_templates", "monitor" ]
  indices:
    - names: ["hass-events*", "active-hass-index-*", "all-hass-events" ]
      privileges: [
        "manage",
        "index",
        "create_index",
        "create"
      ]